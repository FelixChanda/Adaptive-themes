# Signing material — never commit or bundle these
keystore.properties
*.jks
*.keystore

# Build outputs / caches — regenerated by Gradle, should never be shipped in source archives
/build/
**/build/
.gradle/
.kotlin/
local.properties

# IDE
.idea/
*.iml
.DS_Store
